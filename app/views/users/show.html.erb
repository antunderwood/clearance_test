<h1><%= mail_to @user.email if signed_in? %></h1>

<% if signed_in_as_admin? %>
  <div id="control_block">
    <%= link_to "Edit", edit_user_path(@user) %>
    <%= link_to "Users", users_path %>
  </div>
<% end %>
